{
  "name": "sparkfest",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix landing-page",
    "build": "npm run build --prefix landing-page",
    "start": "npm run start --prefix landing-page",
    "install:all": "npm install --prefix landing-page"
  }
}
